machine:
  node:
    version: 4
  post:
    - npm install -g Financial-Times/origami-build-tools#node4
dependencies:
  override:
    - obt install
  cache_directories:
    - "node_modules"
test:
  pre:
    - obt demo
  override:
    - obt test
    - obt verify
    - node pa11yDemo.js
