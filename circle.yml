machine:
  node:
    version: 4
  post:
    - npm install -g Financial-Times/origami-build-tools#node4
    - npm install -g pa11y
dependencies:
  override:
    - obt install
  cache_directories:
    - "node_modules"
test:
  pre:
    - obt demo
  override:
    - obt test
    - obt verify
    - pa11y file://demos/local/standard.html --arguments
